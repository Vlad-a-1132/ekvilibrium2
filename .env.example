# Публичный URL сайта (canonical, Open Graph, sitemap). На проде: https://equilibriumkanz.ru
NEXT_PUBLIC_SITE_URL=https://equilibriumkanz.ru

# Абсолютный путь к корню проекта (рядом с package.json, внутри — папка public/).
# На Ubuntu + pm2 задайте явно, если загрузки из админки не видны на витрине: процесс
# мог стартовать с cwd не из каталога приложения, и файлы писались бы «мимо» public/.
# PROJECT_ROOT=/var/www/equilibrium-web
# Опционально: UPLOAD_PATH_DEBUG=1 — одна строка в лог на каждый POST /api/upload с путями на диске.

DATABASE_URL=postgresql://postgres:Ac76du31@localhost:5432/equilibrium?schema=public

ADMIN_EMAIL=admin@equilibrium.local
ADMIN_PASSWORD=changeme

TELEGRAM_BOT_TOKEN=8535691407:AAGqGo1mcD3dmE7eWdGuPMmF9n1bexBwMWU
TELEGRAM_CHAT_ID=-5284505979
ADMIN_PANEL_LOGIN=jellay
ADMIN_PANEL_PASSWORD=123jellay456
